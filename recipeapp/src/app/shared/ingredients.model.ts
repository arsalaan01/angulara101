export class Ingredient{
  //Alternate way to initialize the name and amount variable of this class directly using constructor by assigning access specifier infront of them see below example
  constructor(public name: string,public amount:number){

  }
}
